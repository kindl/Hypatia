module Example

import Native(setColor, circle)

# Define a vector with two constructors
# A vector has either two or three elements
type Vector a = Vec2 a a | Vec3 a a a

fillCircle (Vec2 x y) r = circle "fill" x y r

draw _ = let { _ = setColor 40 30 240 } in fillCircle (Vec2 400 300) 200
